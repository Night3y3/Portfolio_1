{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "npm install --no-audit --no-fund && (command -v git-lfs >/dev/null 2>&1 && echo 'git-lfs present' || (sudo apt-get update -qq && sudo apt-get install -y -qq git-lfs))"
    },
    "run": {"enabled": true, "command": null},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
